"""Runtime do Trabalho Final — Bora Fibra: do modelo à decisão operacional.

Todo módulo de src/final_project/ importa `config` e, quando fala com a AWS,
`aws`. Esses dois arquivos (e `solution.py`, o parser do quebra-cabeça do
aluno) são a fundação da onda 1 do trabalho final; os demais módulos
(dataset, training, serving, drift, evidence, ...) são entregues por outros
agentes sobre esta base.
"""
